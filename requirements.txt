# Runtime dependencies
aiosqlite>=0.20,<1
httpx>=0.27,<1
neo4j>=5,<7
pydantic>=2.7,<3
PyYAML>=6.0,<7
SQLAlchemy>=2.0.31,<3

# Optional PDF and development dependencies are intentionally excluded.
# In the active Conda environment, install them only when needed with:
# python -m pip install -e ".[pdf]" or ".[dev]"
